import { SearchDireccionPipe } from './search-direccion.pipe';

describe('SearchDireccionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDireccionPipe();
    expect(pipe).toBeTruthy();
  });
});
